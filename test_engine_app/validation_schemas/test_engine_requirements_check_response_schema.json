{
    "$schema": "https://json-schema.org/draft/2019-09/schema",
    "$id": "test_engine_requirements_check_response_schema.json",
    "title": "Test Engine Requirements Check Response Schema",
    "description": "Schema for Test Engine requirements check response",
    "type": "object",
    "properties": {
        "requirement": {
            "description": "The requirement that is used to get compatibility",
            "type": "string"
        },
        "result": {
            "description": "Whether the requirement is successful or not",
            "type": "boolean"
        },
        "errors": {
            "description": "The error messages if result failed",
            "type": "string"
        }
    },
    "required": [
        "requirement",
        "result",
        "errors"
    ]
}