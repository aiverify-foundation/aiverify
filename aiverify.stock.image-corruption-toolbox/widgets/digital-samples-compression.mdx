import React from 'react';
import {DisplayImages} from "./display-samples.mdx"

export const algo_cid = "digital_corruptions"
import {brightness_down_images, brightness_up_images, contrast_down_images, contrast_up_images, saturate_images, random_tilt_images, compression_images} from "./digital-corruption-images.js"

<DisplayImages images = {compression_images} data={props.getResults(algo_cid).results} corruption_index ={6} corruption_name = "Digital: Compression" />

