import React from 'react';
import {DisplayImages} from "./display-samples.mdx"

export const algo_cid = "environment_corruptions"
import {fog_images,snow_images, rain_images} from "./environment-corruption-images.js"

<DisplayImages images = {snow_images} data={props.getResults(algo_cid).results} corruption_index ={1} corruption_name = "Environment: Snow" />

