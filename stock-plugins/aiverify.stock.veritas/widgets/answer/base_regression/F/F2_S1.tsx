import React from "react";

const F2_S1 = ({ props, data }) => {
  return (
    <div>
      The prediction results of the regression model are specific values, which
      have different degrees of deviation compared with the real target variable
      values. The smaller the deviation, the better the prediction effect of the
      model.
    </div>
  );
};

export default F2_S1;
