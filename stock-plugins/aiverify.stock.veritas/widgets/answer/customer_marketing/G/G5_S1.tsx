import React from "react";

const G5_S1 = ({ props, data }) => {
  return (
    <div>
      The overall objective of this marketing system is to maximise profits from
      interest payments, compared to the counterfactual baseline case where the
      marketing system does not exist — where only “walk-in” and online
      customers are offered the product.
    </div>
  );
};

export default G5_S1;
