import React from "react";

const T6 = ({ props, data }) => {
  return (
    <div>
      For customer marketing, limited external transparency is required as the
      activity relates to proactive marketing effort to address potential
      customer needs via a communication channel already approved/consented by
      the customer.
    </div>
  );
};

export default T6;
