import React from "react";

const F3_S1 = ({ props, data }) => {
  return (
    <div>
      For the eligible population (those that should get simplified
      underwriting) the distribution of errors (those that aren’t offered
      simplified underwriting) does not differ by over 20% among subgroups.
    </div>
  );
};

export default F3_S1;
