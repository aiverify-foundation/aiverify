import React from "react";

const G4 = ({ props, data }) => {
  return (
    <div>
      The inventory normally contains model information catalogue (including
      model usage, goals, owners, data controller/processor etc.,) to ensure
      traceability and transparency, organisation-wide model identification
      based on consistent definitions, specifically for AIDA systems with black
      box interpretability requirements, tracking information of validation
      activities and the timeframe of model validity and so on.
    </div>
  );
};

export default G4;
