import React from "react";

const G5_S3 = ({ props, data }) => {
  return (
    <div>
      An example of an external constraint could be missing information when
      customers do not disclose similar services/products with other FSIs.
      Another example would be that FSIs must comply with antitrust and
      competition laws.
    </div>
  );
};

export default G5_S3;
