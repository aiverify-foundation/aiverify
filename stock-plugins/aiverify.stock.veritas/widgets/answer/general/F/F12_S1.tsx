import React from "react";

const F12_S1 = ({ props, data }) => {
  return (
    <div>
      This may include accuracy metrics, primary fairness metric and other
      standard fairness metrics. Disparate impact is the only fairness metric
      that can be measured without a performance period post deployment, as it
      only uses model outcomes and does not require ground truth.
    </div>
  );
};

export default F12_S1;
