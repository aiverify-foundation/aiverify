import React from "react";

const F4_S3 = ({ props, data }) => {
  return (
    <div>
      <b>Proxy bias</b> occurs in cases where input or target variable is
      estimated using a proxy variable which is systematically different from
      the variable of interest.
    </div>
  );
};

export default F4_S3;
