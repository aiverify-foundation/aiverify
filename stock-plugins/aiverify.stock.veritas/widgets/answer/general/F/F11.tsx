import React from "react";

const F11 = ({ props, data }) => {
  return (
    <div>
      Justify the choice of optimal trade-off position. It depends on the
      relative importance of different objectives and priorities, which the
      system is looking to address. For example, minimum acceptable standards
      for AIDA system performance sand fairness standards can be considered.
    </div>
  );
};

export default F11;
