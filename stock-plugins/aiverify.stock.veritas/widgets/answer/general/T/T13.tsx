import React from "react";

const T13 = ({ props, data }) => {
  return (
    <div>
      As an example, to improve communications with the internal model’s users
      (part of internal audience), we can skip a particular feature’s
      explanation – the one which has high weight to the outcome but which is
      not converted to clear action.
    </div>
  );
};

export default T13;
