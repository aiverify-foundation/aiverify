import React from "react";

const G10 = ({ props, data }) => {
  return (
    <div>
      Data pre-processing and engineering may include data cleansing, missing
      data imputation, outlier processing, encoding, and scaling, etc.
    </div>
  );
};

export default G10;
