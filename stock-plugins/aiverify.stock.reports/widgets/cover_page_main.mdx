export const cid = "cover_page_main";

import logo from './logo.png';
import cover from './Cover02.png';
import { getReportDate } from './utils.ts';

export const MyCoverPage = ({ props }) => {

  const subTitleStyles = {
    width:'100%',
    textAlign:'right',
    // paddingRight:'18px',
    fontSize:"12pt",
    // color: '#702F8A',
    textTransform: "uppercase",
    marginBottom: "5px"
  }

  return (
    <div style={{ position:'relative' }}>
      <div style={{ position:'absolute', top:'60px', width:'600px', right:'40px', zIndex:6000, display:'flex', flexFlow:'column wrap', alignItems:'right', textTransform:'uppercase' }}>
        <div style={{ color:'black', fontWeight:600, fontSize:"40pt", textAlign:'right', marginBottom: "10px" }}>{props.properties.reportTitle || "Summary Report"}</div>
        {props.properties.reportSubtitle1 && props.properties.reportSubtitle1.length > 0 && (
          <div style={subTitleStyles} className="c-primary">
            {props.properties.reportSubtitle1}
          </div>
        )}
        {props.properties.reportSubtitle2 && props.properties.reportSubtitle2.length > 0 && (
          <div style={subTitleStyles} className="c-primary">
            {props.properties.reportSubtitle2}
          </div>
        )}
        {props.report && (
          <div style={subTitleStyles} className="c-primary">
            {getReportDate(props)}
          </div>
        )}
      </div>
      <div style={{ position:'absolute', top:0, left:0 }}>
        <img src={cover} alt="cover pattern" height={props.height} />
      </div>
      <div style={{ position:'absolute', top:'960px', right:'30px' }}>
        <img src={logo} alt="AI Verify Logo" width="320px" style={{ padding:0, margin:0 }} />
        <div className="c-primary" style={{ fontWeight:800, fontSize:"8pt", textAlign:'center' }}>AI GOVERNANCE TESTING FRAMEWORK AND TOOLKIT</div>
      </div>
    </div>
  )
}

<div style={{ width:props.width, height: props.height }}>
  <MyCoverPage props={props} />
</div>