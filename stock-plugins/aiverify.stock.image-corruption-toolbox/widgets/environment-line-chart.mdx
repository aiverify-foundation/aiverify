import { CorruptionLineChart } from "./corruptionlinechart.mdx";
export const algo_cid = "aiverify_environment_corruptions";

{
  props.getResults(algo_cid) ? (
    <CorruptionLineChart
        data={props.getResults(algo_cid).results}
        corruption_function="Environment Corruptions"
        width={props.width}
        height={props.height}
      />
  ) : (
    <div>No data</div>
  )
}
