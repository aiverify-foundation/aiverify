import { CorruptionLineChart } from './corruptionlinechart.mdx'
export const algo_cid = "aiverify_blur_corruptions"

{
  props.getResults(algo_cid) ? (
    <CorruptionLineChart
      data={props.getResults(algo_cid).results}
      corruption_function="Blur Corruptions"
      width={props.width}
      height={props.height}
    />
  ) : (
    <div>No data</div>
  )
}
