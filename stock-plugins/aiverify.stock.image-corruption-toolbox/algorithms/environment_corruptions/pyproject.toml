[project]
name = "aiverify_environment_corruptions"
version = "0.10.0"
description = ""
authors = [{ name = "AI Verify" }]
readme = "README.md"
dependencies = ["numpy==1.24.3", "pandas==2.0.3", "pillow==10.2.0", "joblib==1.3.2", "scikit-learn==1.4.0", "scipy==1.12.0", "opencv-python==4.9.0.80", "scikit-image==0.22.0", "wand==0.6.13"]
requires-python = ">=3.10,<3.12"

[project.optional-dependencies]
dev = [
    "test-engine-core @ {root:parent:parent:parent:parent:uri}/test-engine-core"
]

[project.scripts]
aiverify_environment_corruptions = "aiverify_environment_corruptions.__main__:main"

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"