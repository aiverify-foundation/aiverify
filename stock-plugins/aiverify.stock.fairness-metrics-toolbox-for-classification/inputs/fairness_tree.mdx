import { DecisionTree } from "aiverify-shared-library/graph";
import graphdata from "./fairness_tree_graphdata.json";
import definitions from "./fairness_tree_definitions.json";

<DecisionTree
  data={props.data}
  onChangeData={props.onChangeData}
  isEditing={props.isEditing}
  graphdata={graphdata}
  definitions={definitions}
/>
