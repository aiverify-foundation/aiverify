export const metadata = {
  title: 'Checklist Details',
  description: 'View and edit checklist details',
};

export default function ChecklistLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
