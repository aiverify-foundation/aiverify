import { Meta, Story, Canvas, Title, Primary, Controls } from '@storybook/addon-docs';
import { Tooltip } from '.';
import TooltipStories from './tooltip.stories';

<Meta of={TooltipStories} />
<Title>Tooltip React Component</Title>

Display a tooltip when hovering over a trigger element.

<Primary />
<Controls />
